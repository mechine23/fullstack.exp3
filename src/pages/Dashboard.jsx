export default function Dashboard() {
  return (
    <section className="page-block">
      <h2>Dashboard (Protected)</h2>
      <div className="feature-card">
        <p>You are logged in, so this protected area is now accessible.</p>
      </div>
    </section>
  )
}
